/* 识别页面样式 */
.container {
  display: flex;
  flex-direction: column;
  height: 100vh;
  padding: 30rpx;
  box-sizing: border-box;
  background-color: #FFFFFF;
}

/* 防止滚动条 */
page {
  background-color: #FFFFFF;
  height: 100%;
  overflow: hidden;
}

.header {
  text-align: center;
  padding: 30rpx 0;
  margin-bottom: 20rpx;
}

.title {
  font-size: 40rpx;
  font-weight: 600;
  color: #333333;
  font-family: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Helvetica Neue', Arial, sans-serif;
}

.content {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  gap: 50rpx;
  width: 100%;
}

.camera-area {
  width: 680rpx;
  height: 680rpx;
  background-color: #F5F5F5;
  display: flex;
  justify-content: center;
  align-items: center;
  border-radius: 24rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.08);
  border: 1rpx solid #E0E0E0;
  position: relative;
  overflow: hidden;
}

/* 相机区域添加边框装饰 */
.camera-area::before {
  content: '';
  position: absolute;
  top: 20rpx;
  left: 20rpx;
  right: 20rpx;
  bottom: 20rpx;
  border: 2rpx dashed #CCCCCC;
  border-radius: 16rpx;
}

/* 相机图标 */
.camera-area::after {
  content: '📷';
  font-size: 100rpx;
  opacity: 0.3;
}

.action-buttons {
  display: flex;
  gap: 30rpx;
  width: 100%;
  justify-content: center;
  padding-bottom: 40rpx;
}

/* 自定义按钮样式 */
.action-buttons button {
  width: 280rpx;
  height: 90rpx;
  line-height: 90rpx;
  border-radius: 24rpx;
  font-size: 32rpx;
  font-weight: 500;
  margin: 0;
  box-shadow: none;
}

/* 主按钮样式 */
.action-buttons button:first-child {
  background-color: #4CAF50;
  color: #FFFFFF;
  border: none;
}

/* 主按钮激活效果 */
.action-buttons button:first-child:active {
  background-color: #388E3C;
}

/* 次要按钮样式 */
.action-buttons button:last-child {
  background-color: #FFFFFF;
  color: #4CAF50;
  border: 1rpx solid #4CAF50;
}

/* 次要按钮激活效果 */
.action-buttons button:last-child:active {
  background-color: #F1F8E9;
}

/* 加载状态样式 */
.loading {
  color: #666666;
  font-size: 30rpx;
  padding: 20rpx 0;
}

/* 加载动画 */
.loading-spinner {
  width: 40rpx;
  height: 40rpx;
  border: 3rpx solid rgba(76, 175, 80, 0.3);
  border-top-color: #4CAF50;
  border-radius: 50%;
  animation: spin 1s linear infinite;
  margin: 0 auto 16rpx;
}

@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}

/* 识别结果展示样式 */
.result-container {
  width: 100%;
  padding: 30rpx;
  background-color: #F9F9F9;
  border-radius: 24rpx;
  margin-top: 30rpx;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.08);
}

.result-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #333333;
  margin-bottom: 20rpx;
}

.result-content {
  font-size: 28rpx;
  color: #666666;
  line-height: 44rpx;
}